# Analysis + figure pipeline (host-side, no hardware needed).
# Serial data collection has its own light requirements in capture/requirements.txt
# (pyserial); keep these analysis deps minimal so figures/tests run anywhere.
numpy>=1.24
matplotlib>=3.7
pytest>=7.0
