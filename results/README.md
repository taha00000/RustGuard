# results/

Intentionally empty in version control. Trace data (`traces/*.npz`) and figures
(`figures/*.png`) are produced on the hardware bench by the capture and analysis
scripts. The repository ships no measured numbers — every reported result is
generated from a real capture run. See docs/experiment_runbook.md.
