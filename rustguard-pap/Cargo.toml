[package]
name = "rustguard-pap"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "RustGuard Packet Authentication Protocol with reboot-robust nonces."

[dependencies]
rustguard-core = { path = "../rustguard-core" }
heapless = "0.8"
